# 🔧 СРОЧНОЕ ИСПРАВЛЕНИЕ: Ошибка кошелька

## ❌ Проблема
Сервер возвращает ошибку 500 при запросе `/api/wallet`. Причина: **Prisma Client не содержит модель Wallet**.

## ✅ Решение (выполните эти команды)

### Шаг 1: Откройте терминал
Откройте терминал в вашей системе.

### Шаг 2: Перейдите в папку server
```bash
cd /Users/janjan/Desktop/diarai/server
```

### Шаг 3: Сгенерируйте Prisma Client
```bash
npm run prisma:generate
```

**Альтернатива** (если npm не работает):
```bash
./node_modules/.bin/prisma generate
```

Или используйте скрипт:
```bash
./update-prisma.sh
```

### Шаг 4: Перезапустите сервер
Остановите текущий сервер (Ctrl+C), затем:
```bash
npm run dev
```

## ✅ Проверка

После выполнения команд:

1. **В консоли сервера** должно появиться:
   ```
   ✅ Модель Wallet доступна в Prisma Client
   ```

2. **При запросе кошелька** в логах сервера должно быть:
   ```
   [getWallet] Запрос на получение кошелька для пользователя: ...
   [getWallet] Пользователь найден, ID: ...
   [getWallet] Кошелек найден/создан
   ```

3. **В браузере** кошелек должен загрузиться без ошибок.

## 📋 Что уже сделано

✅ Таблица `wallets` создана в базе данных  
✅ Модель Wallet добавлена в `schema.prisma`  
✅ API endpoints созданы  
✅ UI компонент создан  
✅ Обработка ошибок улучшена  

❌ **Prisma Client не обновлен** (нужно выполнить команды выше)

## 🔍 Почему это происходит?

Prisma Client - это сгенерированный код, который создается на основе `schema.prisma`. Когда вы добавляете новую модель в схему, нужно перегенерировать клиент командой `prisma generate`.

## 💡 Полезные команды

- `npm run prisma:generate` - обновить Prisma Client
- `npm run prisma:studio` - открыть Prisma Studio для просмотра данных
- `npm run prisma:migrate` - создать миграцию базы данных

