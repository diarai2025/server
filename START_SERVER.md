# Как запустить сервер

## Быстрый старт

1. Откройте терминал в папке `server`

2. Установите зависимости (если еще не установлены):
```bash
npm install
```

3. Убедитесь, что файл `.env` настроен с правильным `DATABASE_URL`

4. Запустите сервер:
```bash
npm run dev
```

Сервер будет доступен на `http://localhost:3001`

## Проверка работы

После запуска вы должны увидеть:
```
🚀 Сервер запущен на порту 3001
📡 API доступен по адресу http://localhost:3001
💾 Убедитесь, что DATABASE_URL настроен в .env
```

## Возможные проблемы

### Порт 3001 занят
Если порт занят, либо:
- Остановите другой процесс на порту 3001
- Или измените `PORT` в файле `.env`

### Ошибка подключения к базе данных
- Проверьте `DATABASE_URL` в `.env`
- Убедитесь, что пароль в строке подключения правильный
- Проверьте, что Supabase проект активен

### Зависимости не установлены
```bash
cd server
npm install
```

### Prisma клиент не сгенерирован
```bash
npm run prisma:generate
```

## Альтернативный способ запуска

Если `npm` не работает, попробуйте:
```bash
npx tsx watch src/index.ts
```

Или используйте полный путь к node:
```bash
/usr/local/bin/node node_modules/.bin/tsx watch src/index.ts
```

