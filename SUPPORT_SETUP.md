# 📞 Настройка техподдержки

## Шаг 1: Применить миграцию базы данных

### Вариант A: Если таблицы еще не созданы

Выполните обновленный скрипт `CREATE_TABLES.sql` в Supabase SQL Editor. Он уже включает таблицу `support_tickets`.

### Вариант B: Если таблицы уже созданы

Выполните миграцию `prisma/migrations/add_support_tickets.sql` в Supabase SQL Editor:

1. Откройте Supabase SQL Editor
2. Скопируйте содержимое файла `server/prisma/migrations/add_support_tickets.sql`
3. Вставьте и выполните скрипт

## Шаг 2: Обновить Prisma клиент (опционально)

Если используете Prisma напрямую:

```bash
cd server
npx prisma generate
```

## Шаг 3: Перезапустить сервер

```bash
cd server
npm run dev
```

## ✅ Готово!

Техподдержка теперь доступна:
- В меню пользователя на Dashboard есть пункт "Техподдержка"
- Пользователи могут создавать обращения
- Просматривать историю обращений
- Видеть статусы и ответы от поддержки

## API Endpoints

- `GET /api/support` - получить все обращения пользователя
- `GET /api/support/:id` - получить обращение по ID
- `POST /api/support` - создать новое обращение
- `PUT /api/support/:id` - обновить обращение
- `DELETE /api/support/:id` - удалить обращение


