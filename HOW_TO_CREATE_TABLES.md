# 📝 Как создать таблицы в Supabase

## Способ 1: Через Supabase SQL Editor (Рекомендуется)

### Шаг 1: Откройте SQL Editor

1. Перейдите на: https://supabase.com/dashboard/project/hlqlqfeaylfqojypnjcb/sql/new
2. Или: Dashboard → SQL Editor → New query

### Шаг 2: Скопируйте SQL скрипт

Откройте файл `server/CREATE_TABLES.sql` и скопируйте весь его содержимое.

### Шаг 3: Вставьте и выполните

1. Вставьте SQL скрипт в SQL Editor
2. Нажмите кнопку **"Run"** (или `Cmd+Enter` / `Ctrl+Enter`)
3. Дождитесь сообщения "Success. No rows returned"

### Шаг 4: Проверьте создание таблиц

После выполнения вы увидите все созданные таблицы в:
- Dashboard → Table Editor

## Способ 2: Через Prisma (если прямое подключение заработает)

Если прямое подключение начнет работать, можно использовать:

```bash
cd server
source ~/.nvm/nvm.sh
npx prisma db push
```

## ✅ После создания таблиц

Выполните seed для создания демо пользователя:

```bash
cd server
source ~/.nvm/nvm.sh
npm run db:seed
```

Это создаст:
- Email: `demo@example.com`
- Пароль: `demo123`

## 🔍 Проверка

После создания таблиц проверьте подключение:

```bash
cd server
source ~/.nvm/nvm.sh
npm run db:check
```

Должно показать: "✅ Найдено таблиц: 7" (или больше)


