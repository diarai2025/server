# 🔧 Исправление ошибки 500 в техподдержке

## Проблема
При запросе к `/api/support` возникает ошибка 500. Это происходит потому, что:
1. Prisma Client не был перегенерирован после добавления модели `SupportTicket`
2. Таблица `support_tickets` не создана в базе данных

## Решение

### Шаг 1: Создать таблицу в базе данных

Выполните SQL миграцию в Supabase SQL Editor:

1. Откройте Supabase Dashboard → SQL Editor
2. Скопируйте содержимое файла `server/prisma/migrations/add_support_tickets.sql`
3. Вставьте и выполните скрипт (Run или Cmd+Enter)

**Или** если таблицы еще не созданы, выполните обновленный `CREATE_TABLES.sql` (он уже включает таблицу support_tickets).

### Шаг 2: Перегенерировать Prisma Client

```bash
cd server
npx prisma generate
```

Если `npx` не найден, используйте:
```bash
cd server
./node_modules/.bin/prisma generate
```

### Шаг 3: Перезапустить сервер

```bash
cd server
npm run dev
```

## Проверка

После выполнения всех шагов:
1. Откройте приложение
2. Перейдите в Техподдержку через меню пользователя
3. Должен отобразиться пустой список обращений (если обращений еще нет)

## Если проблема сохраняется

Проверьте логи сервера - теперь они показывают более детальную информацию об ошибке.


