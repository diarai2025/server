# Backend Server для DIAR AI Marketing CRM

Backend сервер на Node.js + Express + Prisma + Supabase PostgreSQL.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd server
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```env
DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"
PORT=3001
NODE_ENV=development
```

**Как получить DATABASE_URL:**
1. Откройте ваш проект на [app.supabase.com](https://app.supabase.com)
2. Перейдите в **Settings** → **Database**
3. Найдите **Connection string** → **URI**
4. Замените `[YOUR-PASSWORD]` на ваш пароль базы данных

### 3. Настройка Prisma

```bash
# Генерация Prisma клиента
npm run prisma:generate

# Применение миграций (создание таблиц в БД)
npm run prisma:migrate

# Заполнение тестовыми данными (опционально)
npm run prisma:seed
```

### 4. Запуск сервера

```bash
# Режим разработки (с автоперезагрузкой)
npm run dev

# Продакшен
npm run build
npm start
```

Сервер будет доступен по адресу: `http://localhost:3001`

## 📁 Структура проекта

```
server/
├── prisma/
│   ├── schema.prisma      # Prisma схема БД
│   └── seed.ts            # Заполнение тестовыми данными
├── src/
│   ├── controllers/       # Контроллеры (бизнес-логика)
│   ├── routes/            # Маршруты API
│   ├── middleware/        # Middleware (auth, error handling)
│   ├── db/
│   │   └── prisma.ts      # Prisma клиент
│   └── index.ts           # Точка входа
└── package.json
```

## 🗄️ Модели данных

### User
- Пользователи системы

### Lead
- Лиды (потенциальные клиенты)
- Связан с User, Deal, Task, Message

### Deal
- Сделки
- Связан с User, Lead

### Task
- Задачи
- Связан с User, Lead

### Message
- Сообщения в чате с лидами
- Связан с Lead

### Campaign
- Рекламные кампании

## 🔌 API Endpoints

### Leads (Лиды)
- `GET /api/leads` - Получить все лиды
- `GET /api/leads/:id` - Получить лид по ID
- `POST /api/leads` - Создать лид
- `PUT /api/leads/:id` - Обновить лид
- `DELETE /api/leads/:id` - Удалить лид

### Deals (Сделки)
- `GET /api/deals` - Получить все сделки
- `GET /api/deals/:id` - Получить сделку по ID
- `POST /api/deals` - Создать сделку
- `PUT /api/deals/:id` - Обновить сделку
- `DELETE /api/deals/:id` - Удалить сделку

### Tasks (Задачи)
- `GET /api/tasks` - Получить все задачи
- `GET /api/tasks/:id` - Получить задачу по ID
- `POST /api/tasks` - Создать задачу
- `PUT /api/tasks/:id` - Обновить задачу
- `DELETE /api/tasks/:id` - Удалить задачу

### CRM
- `GET /api/crm/stats` - Статистика CRM
- `GET /api/crm/all` - Все данные CRM (leads, deals, tasks)

### Dashboard
- `GET /api/dashboard/stats` - Статистика для дашборда

### Campaigns
- `GET /api/campaigns` - Получить все кампании
- `GET /api/campaigns/:id` - Получить кампанию по ID
- `POST /api/campaigns` - Создать кампанию
- `PUT /api/campaigns/:id` - Обновить кампанию
- `DELETE /api/campaigns/:id` - Удалить кампанию

## 🔐 Аутентификация

В режиме разработки используется `devAuthMiddleware`, который принимает заголовки:
- `x-user-id` - ID пользователя
- `x-user-email` - Email пользователя

**Пример запроса:**
```bash
curl -H "x-user-id: user-123" \
     -H "x-user-email: user@example.com" \
     http://localhost:3001/api/leads
```

В продакшене замените `devAuthMiddleware` на `authMiddleware` для проверки JWT токенов из Supabase.

## 🛠️ Команды

```bash
# Разработка
npm run dev

# Сборка
npm run build

# Запуск продакшена
npm start

# Prisma
npm run prisma:generate    # Генерация клиента
npm run prisma:migrate      # Применение миграций
npm run prisma:studio       # Открыть Prisma Studio
npm run prisma:seed         # Заполнить тестовыми данными
```

## 📝 Примечания

- Все контроллеры используют Prisma вместо mock данных
- Данные изолированы по пользователям (userId)
- Используется каскадное удаление для связанных данных
- В режиме разработки логируются все запросы

